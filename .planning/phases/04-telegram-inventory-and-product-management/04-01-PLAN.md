---
phase: 04-telegram-inventory-and-product-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/telegram/webhook/route.ts
  - src/lib/telegram.ts
autonomous: true
requirements:
  - BOT-INV-01
  - BOT-PROD-01

must_haves:
  truths:
    - "When admin confirms payment with /send <OrderID> 1 or /reply <OrderID> 1, item stocks are deducted from database according to ordered quantities"
    - "Stock count never drops below 0"
    - "Admin receives Telegram message confirming order status and updated inventory balances"
  artifacts:
    - path: "src/app/api/telegram/webhook/route.ts"
      provides: "Order status completion handling and automatic stock deduction logic"
  key_links:
    - from: "src/app/api/telegram/webhook/route.ts"
      to: "src/lib/schema.ts"
      via: "db.update(products).set({ stock: newStock })"
      pattern: "db\\.update\\(products\\)"
---

<objective>
Implement automatic inventory stock deduction in the database when payment is confirmed via Telegram bot (/send <OrderID> 1), with real-time stock balance reporting in the bot reply.

Purpose: Ensure inventory accuracy when orders are approved by admin via Telegram.
Output: Updated webhook route with order item parsing and product stock deduction.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/schema.ts
@src/lib/db.ts
@src/app/api/telegram/webhook/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add automated stock deduction on payment confirmation</name>
  <files>src/app/api/telegram/webhook/route.ts</files>
  <action>
    In `src/app/api/telegram/webhook/route.ts`, locate the `statusArg === "1"` block (Payment Success / Code 1).
    Per D-01:
    1. Parse `matchedOrder.itemsJson`.
    2. For each ordered item, find the corresponding product in the database by `item.id` (or fallback by `item.name`).
    3. Calculate deducted stock: `Math.max(0, (existingProduct.stock ?? 0) - item.quantity)`.
    4. Update the `stock` in `products` table using `db.update(products).set({ stock: newStock }).where(eq(products.id, existingProduct.id))`.
    5. Collect an inventory update summary string (e.g., `• Product Name: 10 -> 8 (x2)`).
    6. Include the inventory update summary in the admin confirmation message sent via Telegram bot.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Payment success via `/send <OrderID> 1` deducts product stock in DB and reports updated stock count to admin chat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram Webhook -> Next.js API | Untrusted payload from external webhook caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | webhook route | mitigate | Validate `x-telegram-bot-api-secret-token` and verify `chatId` matches `TELEGRAM_ADMIN_CHAT_ID` |
| T-04-02 | Denial of Service | stock update | mitigate | Use `Math.max(0, ...)` to prevent negative stocks and wrap in try/catch to avoid unhandled crashes on invalid item JSON |
</threat_model>

<verification>
Run TypeScript compilation and verify error-free build:
`npx tsc --noEmit`
</verification>

<success_criteria>
1. Code 1 payment approvals automatically reduce product stock according to quantities purchased.
2. Negative stock values are clamped at 0.
3. Bot sends inventory summary feedback to admin chat.
</success_criteria>

<output>
Create `.planning/phases/04-telegram-inventory-and-product-management/04-01-SUMMARY.md` after completion.
</output>
