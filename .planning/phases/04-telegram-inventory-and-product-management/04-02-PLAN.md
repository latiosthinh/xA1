---
phase: 04-telegram-inventory-and-product-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/api/telegram/webhook/route.ts
autonomous: true
requirements:
  - BOT-PROD-02
  - BOT-PROD-03
  - BOT-PROD-04
  - BOT-PROD-05

must_haves:
  truths:
    - "Admin can send /products or /list in Telegram to see all products with price, stock, and status"
    - "Admin can send /product <id> to view full details of a single product"
    - "Admin can send /addproduct <name> | <price> | <stock> | [imageUrl] | [description] to create a product directly from Telegram"
    - "Admin can send /setstock <id> <stock> to immediately update item stock"
    - "Admin can send /editproduct <id> | <name> | <price> | <stock> | [imageUrl] | [description] to update a product"
    - "Admin can send /delproduct <id> to delete a product"
    - "Admin can send /help to view all available commands"
    - "Product listing and product detail render with inline keyboard buttons (pagination, stock +/-, edit, delete) that update the same message in place via editMessageText"
    - "Callback queries are authorized against TELEGRAM_ADMIN_CHAT_ID and unauthorized callbacks are answered with a rejection alert"
  artifacts:
    - path: "src/app/api/telegram/webhook/route.ts"
      provides: "Full Telegram bot command dispatcher for product and inventory CRUD"
  key_links:
    - from: "src/app/api/telegram/webhook/route.ts"
      to: "src/lib/schema.ts"
      via: "db operations on products table"
      pattern: "db\\.(select|insert|update|delete)\\(products\\)"
---

<objective>
Implement full in-chat product management commands (/products, /product, /addproduct, /setstock, /editproduct, /delproduct, /help) in the Telegram bot webhook for convenient admin control from mobile, plus a lightweight inline-keyboard UI for browsing and editing products without typing commands.

Purpose: Enable full store inventory and product catalog management directly through Telegram without opening the web dashboard.
Output: Extended webhook handler supporting interactive CRUD commands and an inline-keyboard UI.
</objective>

<execution_context>
@$HOME/.config/antigravity/get-shit-done/workflows/execute-plan.md
@$HOME/.config/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/schema.ts
@src/lib/db.ts
@src/lib/currency.ts
@src/lib/telegram.ts
@src/app/api/telegram/webhook/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add product listing, viewing, and help commands to Telegram webhook</name>
  <files>src/app/api/telegram/webhook/route.ts</files>
  <action>
    In `src/app/api/telegram/webhook/route.ts`:
    1. Verify sender authorization against `process.env.TELEGRAM_ADMIN_CHAT_ID`. If not matched, reject/ignore unauthorized commands.
    2. Implement `/help` and `/start`: Return a structured guide with Markdown formatting listing all commands:
       - Order fulfillment: `/send <OrderID> 1` (Success + auto deduct stock), `/send <OrderID> 0` (Reject), `/send <OrderID> 2` (Verify), `/send <OrderID> <Message>`
       - Product query: `/products` (or `/list`), `/product <id>` (or `/view <id>`)
       - Product mutation: `/addproduct`, `/setstock`, `/editproduct`, `/delproduct`
       - Announcement: `/broadcast <message>`
    3. Implement `/products` / `/list`: Query `products` table ordered by `createdAt desc`. Format each product as:
       - `📦 *[Name]* (ID: \`id\`)`
       - `💰 Price: [formatDualPrice] | 📊 Stock: [stock] ([IN STOCK / OUT OF STOCK])`
       Handle empty catalog gracefully.
    4. Implement `/product <id>` / `/view <id>`: Query `products` table where `id = <id>`. If not found, reply with error; if found, render detailed view including description, price, stock, and image URL.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can list all products, view single product details, and get command help via Telegram.</done>
</task>

<task type="auto">
  <name>Task 2: Add product creation, stock modification, editing, and deletion commands</name>
  <files>src/app/api/telegram/webhook/route.ts</files>
  <action>
    In `src/app/api/telegram/webhook/route.ts`:
    1. Implement `/setstock <id> <stock>`:
       - Validate `<stock>` is a non-negative integer (`Math.max(0, parseInt(stockArg))`).
       - Update `products` record.
       - Send confirmation message with new stock level.
    2. Implement `/addproduct <name> | <price> | <stock> | [imageUrl] | [description]`:
       - Parse pipe-delimited parameters: `name`, `price` (number), `stock` (integer, default 0), optional `imageUrl`, optional `description`.
       - Validate name and price. If invalid or missing, reply with exact syntax guide.
       - Insert new product using `crypto.randomUUID()`.
       - Reply with success message and new product ID.
    3. Implement `/editproduct <id> | <name> | <price> | <stock> | [imageUrl] | [description]`:
       - Check if product exists.
       - Update fields provided.
       - Reply with updated product card.
    4. Implement `/delproduct <id>`:
       - Delete product with matching `id`.
       - Reply with deletion confirmation.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can create, edit, update stock, and delete products directly from Telegram chat.</done>
</task>

<task type="auto">
  <name>Task 3: Add inline-keyboard UI for browsing and editing products in place</name>
  <files>src/app/api/telegram/webhook/route.ts</files>
  <action>
    In `src/app/api/telegram/webhook/route.ts`:
    1. Attach inline keyboards to `/products` listing and `/product <id>` detail views. Use stable `callback_data` strings, e.g.:
       - Listing: `list:<page>` pagination (`◀ Prev`, `Next ▶`), one button per product row `📦 <name>`.
       - Detail: `stock:<id>:+` / `stock:<id>:-` to increment/decrement by 1, `edit:<id>` (prompts for `/editproduct` syntax), `del:<id>` (shows a confirm keyboard), `back:list`.
    2. Add a `callback_query` handler. Re-authorize `chatId` against `TELEGRAM_ADMIN_CHAT_ID`; for unauthorized callbacks answer with a rejection alert and stop.
    3. Route each callback by its prefix, perform the DB op, then refresh the same message with `editMessageText` (re-render the list or detail view) so the UI updates in place. Acknowledge with `answerCallbackQuery`.
    4. For `del:<id>`, require confirmation via a `del:confirm:<id>` button before deleting; `back:list` returns to page 1 of the listing.
    5. Guard against negative stock on `stock:<id>:-` (floor at 0) and handle out-of-range pages gracefully.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Admin can browse, paginate, adjust stock, edit, and delete products via inline keyboard buttons that update the message in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram Chat -> Bot Commands | Command execution triggered by chat messages |
| Telegram Chat -> Callback Queries | Button presses (`callback_query`) triggering stock mutation, edit, and delete actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | bot command dispatcher | mitigate | Strict check verifying `String(chatId) === String(process.env.TELEGRAM_ADMIN_CHAT_ID)` before executing any admin command or callback action |
| T-04-04 | Tampering | input parser | mitigate | Sanitize and validate numeric price/stock values with `Number()` and `Math.max(0, ...)` before DB write |
</threat_model>

<verification>
Run TypeScript check and ensure everything compiles cleanly:
`npx tsc --noEmit`
</verification>

<success_criteria>
1. All Telegram product management commands (`/products`, `/product`, `/addproduct`, `/setstock`, `/editproduct`, `/delproduct`, `/help`) work seamlessly.
2. Only authorized admin chat can execute product management actions or trigger callback buttons.
3. Accurate feedback messages are returned for each action.
4. Inline keyboards on listing/detail update the same message in place (pagination, stock +/−, edit, delete with confirm, back).
</success_criteria>

<output>
Create `.planning/phases/04-telegram-inventory-and-product-management/04-02-SUMMARY.md` after completion.
</output>
